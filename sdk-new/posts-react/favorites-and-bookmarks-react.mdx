---
title: "Favorites and Bookmarks (React)"
description: " "
---

Bookmark posts explicitly for easy reference:

```typescript
import { useUser } from '@sageprotocol/sdk/react';
import { useWallet } from '@suiet/wallet-kit';

function FavoritePost({ ownedUserId, authorSharedUserId, postId }) {
  const { addFavoritePost } = useUser();
  const { account } = useWallet();

  async function handleFavorite() {
    await addFavoritePost({
      appId: 'your-app-id',
      network: 'testnet',
      authorSharedUserId,
      postId,
      ownedUserId
    });
    console.log("Post bookmarked!");
  }

  return <button onClick={handleFavorite}>Bookmark Post</button>;
}
```