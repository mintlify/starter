---
title: "List companies"
openapi: "GET /public/companies"
---

Lists companies visible to the authenticated partner or affiliate API key.

Use this endpoint when you need to recover a `companyId` created earlier with `POST /public/company` before starting an incorporation, creating a service, or calling company-scoped Commenda OS endpoints.
