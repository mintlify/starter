---
title: "Access"
description: "@sage/admin > access"
---

```rust
use std::type_name;
use sage_admin::admin;
```

# Structs

### Struct ChannelConfig

Holds the `Channel` type for runtime type checks.

```rust
public struct ChannelConfig has key
```

<Expandable title="Fields">
  ```rust
  id: UID,
  type_name: TypeName
  ```
</Expandable>

### Struct ChannelWitnessConfig

Holds the `ChannelWitness` type for runtime type checks.

```rust
public struct ChannelWitnessConfig has key
```

<Expandable title="Fields">
  ```rust
  id: UID,
  type_name: TypeName
  ```
</Expandable>

### Struct GroupWitnessConfig

Holds the `GroupWitness` type for runtime type checks.

```rust
public struct GroupWitnessConfig has key
```

<Expandable title="Fields">
  ```rust
  id: UID,
  type_name: TypeName
  ```
</Expandable>

### Struct UserOwnedConfig

Holds the `UserOwned` type for runtime type checks.

```rust
public struct UserOwnedConfig has key
```

<Expandable title="Fields">
  ```rust
  id: UID,
  type_name: TypeName
  ```
</Expandable>

### Struct UserSharedConfig

Holds the `UserShared` type for runtime type checks.

```rust
public struct UserSharedConfig has key
```

<Expandable title="Fields">
  ```rust
  id: UID,
  type_name: TypeName
  ```
</Expandable>

### Struct UserWitnessConfig

Holds the `UserWitness` type for runtime type checks.

```rust
public struct UserWitnessConfig has key
```

<Expandable title="Fields">
  ```rust
  id: UID,
  type_name: TypeName
  ```
</Expandable>


# Constants

Runtime type checking failed for the struct type.

```rust
const ETypeMismatch: u64 = 370;
```

Runtime type checking failed for the witness.

```rust
const EWitnessMismatch: u64 = 371;
```

# Functions

### Function `assert_channel`

```
assert_channel<ChannelType: key>(channel_config: &ChannelConfig, channel: &ChannelType)
```

<Expandable title="Implementation">
  ```rust
    public fun assert_channel<ChannelType: key> (
        channel_config: &ChannelConfig,
        channel: &ChannelType
    ) {
        let is_auth = verify_channel<ChannelType>(
            channel_config,
            channel
        );
    
        assert!(is_auth, ETypeMismatch);
    }
  ```
</Expandable>

### Function `assert_channel_witness`

```
assert_channel_witness<ChannelWitnessType: drop>(channel_witness_config: &ChannelWitnessConfig, channel_witness: &ChannelWitnessType)
```

<Expandable title="Implementation">
  ```rust
    public fun assert_channel_witness<ChannelWitnessType: drop> (
      channel_witness_config: &ChannelWitnessConfig,
      channel_witness: &ChannelWitnessType
    ) {
      let is_auth = verify_channel_witness<ChannelWitnessType>(
          channel_witness_config,
          channel_witness
      );
    
      assert!(is_auth, EWitnessMismatch);
    }
  ```
</Expandable>

### Function `assert_group_witness`

```
assert_group_witness<GroupWitnessType: drop>(group_witness_config: &GroupWitnessConfig, group_witness: GroupWitnessType)
```

<Expandable title="Implementation">
  ```rust
    public fun assert_group_witness<GroupWitnessType: drop> (
      group_witness_config: &GroupWitnessConfig,
      group_witness: &GroupWitnessType
    ) {
      let is_auth = verify_group_witness<ChannelWitnessType>(
          group_witness_config,
          group_witness
      );
    
      assert!(is_auth, EWitnessMismatch);
    }
  ```
</Expandable>










- assert_owned_user\<T\>(owned_user__config: &\_OwnedUserConfig, owned_user: T)
- assert_shared_user\<T\>(shared_user__config: &\_SharedUserConfig, shared_user: T)
- assert_user_witness\<T\>(user_witness__config: &\_UserWitnessConfig, user_witness: T)
- verify_channel\<T\>(channel__config: &\_ChannelConfig, channel: T): bool
- verify_channel_witness\<T\>(channel__witness_config: &\_ChannelWitnessConfig, channel_witness: T): bool
- verify_group_witness\<T\>(group__witness_config: &\_GroupWitnessConfig, group_witness: T): bool
- verify_owned_user\<T\>(owned_user__config: &\_OwnedUserConfig, owned_user: T): bool
- verify_shared_user\<T\>(shared_user__config: &\_SharedUserConfig, shared_user: T): bool

verify_user_witness\<T\>(user_witness__config: &\_UserWitnessConfig, user_witness: T): bool